{
  "name": "world-cup-2026-prediction-bracket",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "mkdir -p dist/assets dist/vendor && cp index.html styles.css app.js template-data.js \"Tippin VM 2026 -mal.xlsx\" dist/ && cp assets/* dist/assets/ && cp -R vendor/* dist/vendor/"
  }
}
